- generate events again with 'TimeShower:QEDshowerByGamma = On ' (DONE)

- plot average multiplicity:
"It should be something like

<n_PID> = sum_event n_PID^event * wgt^event

where n_PID^event is the number of PID="PID" in event="event" and the event weight wgt^event is normalized by

xsec = sum_event wgt^event" (DONE?)

- turn notebook into python script